# Rosencharts Prompts

One `.md` file per chart type. Loaded dynamically when that chart type is selected.

**Color system:** Tailwind classes or hex. Use `color`, `colorFrom`/`colorTo`, or `multipleColors` — never `fill` or `_seriesColors` (those are Shadcn).
